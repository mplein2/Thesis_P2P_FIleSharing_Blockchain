Index: p2p.py
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>import socket\r\nimport threading\r\nimport time\r\n\r\ndef receiver():\r\n    while True:\r\n        # Get UDP From here.\r\n        UDP_PORT = 6700\r\n        sock = socket.socket(socket.AF_INET,  # Internet\r\n                             socket.SOCK_DGRAM)  # UDP\r\n        sock.bind((\"\", UDP_PORT))\r\n        y = threading.Thread(target=keepAlive,args=[sock])\r\n        y.start()\r\n        while True:\r\n            print(\"Trying to receive\")\r\n            data, addr = sock.recvfrom(1024)  # buffer size is 1024 bytes\r\n            print(\"Received:\", data, \" from\", addr)\r\n\r\n\r\n# peers should be a list\r\ndef keepAlive(sock):\r\n    peers = [\"82.102.57.157\", ]\r\n    while True:\r\n        time.sleep(5)\r\n        for peer in peers:\r\n            print(\"Sending KeepAlive to \",peer,\":\",6700)\r\n            sock.sendto(b'KeepAlive', (peer,6700))\r\n        sock.sendto(b'Data', (peer,6700))\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    x = threading.Thread(target=receiver)\r\n    x.start()\r\n\r\n\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/p2p.py b/p2p.py
--- a/p2p.py	(revision 7ad61591d2bd64befc0e8a7f9e768e44fab209a3)
+++ b/p2p.py	(date 1644421423072)
@@ -2,14 +2,15 @@
 import threading
 import time
 
+
 def receiver():
     while True:
         # Get UDP From here.
-        UDP_PORT = 6700
+        UDP_PORT = 56700
         sock = socket.socket(socket.AF_INET,  # Internet
                              socket.SOCK_DGRAM)  # UDP
         sock.bind(("", UDP_PORT))
-        y = threading.Thread(target=keepAlive,args=[sock])
+        y = threading.Thread(target=keepAlive, args=[sock])
         y.start()
         while True:
             print("Trying to receive")
@@ -23,13 +24,11 @@
     while True:
         time.sleep(5)
         for peer in peers:
-            print("Sending KeepAlive to ",peer,":",6700)
-            sock.sendto(b'KeepAlive', (peer,6700))
-        sock.sendto(b'Data', (peer,6700))
+            print("Sending KeepAlive to ", peer, ":", 56700)
+            sock.sendto(b'KeepAlive', (peer, 56700))
+        sock.sendto(b'Data', (peer, 56700))
 
 
 if __name__ == "__main__":
     x = threading.Thread(target=receiver)
     x.start()
-
-
